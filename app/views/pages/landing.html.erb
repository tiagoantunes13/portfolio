<main class="flex-grow">
  <!-- Hero Section -->
  <div class="@container px-4 sm:px-8 md:px-20 lg:px-40 py-10 md:py-20">
    <div class="flex flex-col gap-6 max-w-4xl">
      <div class="flex flex-col gap-2">
        <h1 class="text-white text-4xl font-black leading-tight tracking-[-0.033em] @[480px]:text-5xl">
          Tiago Antunes // AI-Driven Software Engineer
        </h1>
        <h2 class="text-primary text-sm font-normal leading-normal @[480px]:text-base">
          Building intelligent web solutions with Ruby on Rails and modern AI frameworks. Specializing in LLMs, RAG systems, and AI-powered automation to transform complex workflows into seamless user experiences.
        </h2>
      </div>
      <div class="flex-wrap gap-3 flex">
        <%= link_to about_path, class: "flex min-w-[84px] max-w-[480px] cursor-pointer items-center justify-center overflow-hidden rounded h-10 px-4 @[480px]:h-12 @[480px]:px-5 bg-primary text-background-dark text-sm font-bold leading-normal tracking-[0.015em] @[480px]:text-base hover:bg-accent-secondary hover:text-white transition-colors" do %>
          <span class="truncate">Read my README.md</span>
        <% end %>
        <%= link_to contact_path, class: "flex min-w-[84px] max-w-[480px] cursor-pointer items-center justify-center overflow-hidden rounded h-10 px-4 @[480px]:h-12 @[480px]:px-5 bg-white/10 text-white text-sm font-bold leading-normal tracking-[0.015em] @[480px]:text-base hover:bg-white/20 transition-colors" do %>
          <span class="truncate">Get In Touch</span>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Featured Projects Section -->
  <div>
    <h2 class="text-primary text-lg sm:text-xl md:text-[22px] font-bold leading-tight tracking-[-0.015em] px-4 sm:px-8 md:px-20 lg:px-40 pb-3 pt-5">ls -l featured/</h2>
  </div>

  <% if @projects.any? %>
    <div class="grid grid-cols-[repeat(auto-fit,minmax(240px,1fr))] gap-4 px-4 sm:px-8 md:px-20 lg:px-40 pb-6">
      <% @projects.each do |project| %>
        <%= link_to project_path(project), class: "flex flex-col gap-3 pb-3 group" do %>
          <div class="w-full bg-center bg-no-repeat aspect-video bg-contain rounded-lg overflow-hidden transition-all duration-300">
            <% if project.primary_image.present? %>
              <div class="w-full h-full bg-contain bg-center bg-no-repeat transition-transform duration-300 group-hover:scale-105" style='background-image: url("<%= url_for(project.primary_image) %>");' data-alt="<%= project.title %> screenshot"></div>
            <% else %>
              <div class="w-full h-full bg-gray-800 flex items-center justify-center transition-transform duration-300 group-hover:scale-105">
                <span class="text-gray-600 text-sm">No image</span>
              </div>
            <% end %>
          </div>
          <div>
            <p class="text-white text-base font-medium leading-normal">
              <%= project.title.downcase.gsub(' ', '_') %>.rb
              <span class="text-accent-secondary text-xs ml-1">★</span>
            </p>
            <% if project.technologies.present? %>
              <p class="text-primary/70 text-xs sm:text-sm font-normal leading-normal">Dependencies: <%= project.technologies %></p>
            <% end %>
            <p class="text-white/60 text-sm font-normal leading-relaxed line-clamp-2"><%= project.description %></p>
            <div class="flex items-center gap-3 mt-2">
              <span class="text-primary group-hover:text-accent-secondary text-xs font-medium transition-colors">[ view details ]</span>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- View All Projects Button -->
    <div class="px-4 sm:px-8 md:px-20 lg:px-40 pb-10">
      <%= link_to projects_path, class: "inline-flex items-center justify-center gap-2 text-primary hover:text-accent-secondary text-sm font-medium transition-colors" do %>
        <span>View All Projects</span>
        <span>→</span>
      <% end %>
    </div>
  <% else %>
    <div class="px-4 sm:px-8 md:px-20 lg:px-40 pb-10">
      <div class="bg-black/20 p-6 rounded-lg border border-primary/20">
        <p class="text-white/60 font-mono text-sm">
          No featured projects. Mark projects as featured via Rails console.
        </p>
      </div>
    </div>
  <% end %>

  <!-- About Section -->
  <div>
    <h2 class="text-primary text-lg sm:text-xl md:text-[22px] font-bold leading-tight tracking-[-0.015em] px-4 sm:px-8 md:px-20 lg:px-40 pb-3 pt-5">cat about.me/README.md</h2>
  </div>

  <div class="px-4 sm:px-8 md:px-20 lg:px-40 pb-10">
    <div class="bg-black/20 p-4 sm:p-6 rounded-lg border border-primary/20">
      <p class="text-white/80 font-mono text-xs sm:text-sm leading-relaxed">
        Hello, I'm Tiago Antunes, a software engineer specializing in AI-driven web solutions and Ruby on Rails development. With a Master's degree in Software Engineering from the University of Coimbra, I focus on leveraging artificial intelligence to automate complex workflows, improve content evaluation accuracy, and enhance user experiences.
        <br/><br/>
        My expertise includes building AI-powered classification systems using LLMs, implementing Retrieval-Augmented Generation (RAG) approaches with vector embeddings, and developing conversational interfaces with AI function-calling. I'm passionate about exploring emerging AI technologies and continuously learning to push the boundaries of what's possible with intelligent automation.
        <br/><br/>
      </p>
    </div>
  </div>
</main>
